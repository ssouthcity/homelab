apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
spec:
  interval: 15m
  chart:
    spec:
      chart: metallb
      version: 0.x
      sourceRef:
        kind: HelmRepository
        name: metallb-charts
  values:
