apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager-webhook-vercel-release
spec:
  interval: 5m
  chart:
    spec:
      chart: deploy/cert-manager-webhook-vercel
      sourceRef:
        kind: GitRepository
        name: cert-manager-webhook-vercel-repo
      version: 1.x
  values:
    groupName: acme.southcity.dev
