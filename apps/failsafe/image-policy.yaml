apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: failsafe-discord-bot
spec:
  imageRepositoryRef:
    name: failsafe-discord-bot
  policy:
    semver:
      range: vx.x.x
