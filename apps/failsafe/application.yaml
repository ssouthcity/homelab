apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: failsafe
spec:
  sourceRef:
    kind: GitRepository
    name: failsafe
  path: ./k8s
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: failsafe-age
