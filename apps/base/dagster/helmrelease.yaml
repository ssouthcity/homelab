apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dagster
spec:
  releaseName: dagster
  interval: 1h
  chart:
    spec:
      chart: dagster
      version: ~1
      sourceRef:
        kind: HelmRepository
        name: dagster
