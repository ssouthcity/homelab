apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-media-pvc
  namespace: longhorn-system
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
