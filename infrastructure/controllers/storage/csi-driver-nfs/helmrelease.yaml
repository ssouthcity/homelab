apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
spec:
  interval: 1h
  chart:
    spec:
      chart: csi-driver-nfs
      version: ~4
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
  values: {}
